module.exports = {
	makeObjFollower : function(obj, name) {
		obj.follower = name
	},
	removeFollower : function(obj) {
		delete obj.follower
	},
	getCurrent : function(obj) {
		if (obj == undefined)
			return undefined
		return obj.follower
	}
}
