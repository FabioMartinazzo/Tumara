var clone = require('clone')

var KeyValue = {
	key : null,
	value : null, 
	init : function(key, value) {
		this.key = key
		this.value = value
		return clone(this)
	}
}

module.exports = {
	getNew : function(key, value) {
		return KeyValue.init(key, value)
	}
}
